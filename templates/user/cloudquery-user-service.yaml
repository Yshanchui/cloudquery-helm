apiVersion: v1
kind: Service
metadata:
  labels:
    app: cloudquery-user
  name: cloudquery-user
spec:
  ports:
    - name: "http"
      port: 8001
      targetPort: 8001
  selector:
    app: cloudquery-user